import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.petshop.managing",
  appName: "PetShop Managing Apps",
  webDir: "dist/client",
};

export default config;
